apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fleet-members-init
  namespace: argocd
spec:
  destination:
    name: in-cluster
    namespace: argocd
  project: default
  sources:
    - path: bootstrap/control-plane/members-init
      #path: gitops/fleet/bootstrap/control-plane/members-init
      ##https://github.com/aws-samples/fleet-management-on-amazon-eks-workshop
      repoURL: >-        
        https://d1t8il14om2k8l.cloudfront.net/gitea/workshop-user/eks-fleet-workshop-gitops-fleet
      targetRevision: HEAD
  syncPolicy:
    automated:
      allowEmpty: true
      selfHeal: true
    retry:
      limit: 100
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true